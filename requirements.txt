# Multi-Modal Bar Tuning Optimization - Requirements
# Install with: pip install -r requirements.txt

numpy>=1.20.0
scipy>=1.7.0

# Optional: PyPardiso for faster sparse solves (requires Intel MKL)
# Install with: pip install pypardiso
# pypardiso>=0.4.0

# Optional: PRIMME for faster eigenvalue solves
# Install with: pip install primme
# primme>=3.2.0
